# 이더리움 세폴리아 기반 투표 시스템 PRD/SRS 및 화면 설계서

## 1. 문서 개요

### 1.1 목적

본 문서는 이더리움 세폴리아(Sepolia) 테스트넷과 메타마스크(MetaMask)를 연동하여 웹 브라우저에서 사용할 수 있는 투표 시스템의 제품 요구사항(PRD), 소프트웨어 요구사항(SRS), 화면 설계서를 정의한다.

### 1.2 범위

본 시스템은 다음 기능을 제공한다.

- 웹 브라우저 기반 투표 서비스
- 크롬 브라우저 확장 프로그램 메타마스크 연동
- 이더리움 세폴리아 네트워크 연동
- 후보자 등록, 수정, 삭제
- 후보자 이름 및 후보자 사진 등록
- 관리자 수동 투표 시작 및 종료
- 후보자 최소 2명 이상일 때 투표 시작
- 지갑 주소 기준 1인 1투표
- 중복 투표 방지
- 투표 진행 중 현재 득표수 공개
- 투표 종료 후 전체 득표수 및 결과 공개

### 1.3 확정 정책

| 항목 | 결정 사항 |
|---|---|
| 후보자 사진 저장 방식 | 서버 저장 |
| 관리자 주소 설정 방식 | 스마트 컨트랙트 배포자 주소 고정 |
| 투표 기간 관리 방식 | 관리자가 수동으로 시작 및 종료 |
| 1인 1투표 기준 | 지갑 주소 기준 |
| 후보자 수정/삭제 | 투표 시작 전 허용 |
| 투표 운영 방식 | 단일 투표 |
| 득표수 공개 정책 | 공개 투표, 진행 중 득표수 공개 |

### 1.4 용어 정의

| 용어 | 설명 |
|---|---|
| 세폴리아 | 이더리움 테스트넷 중 하나 |
| 메타마스크 | 크롬 브라우저 확장 지갑 |
| 스마트 컨트랙트 | 투표 상태, 후보자, 투표 내역, 득표수를 관리하는 블록체인 프로그램 |
| 관리자 | 스마트 컨트랙트를 배포한 지갑 주소의 사용자 |
| 유권자 | 메타마스크 지갑으로 투표에 참여하는 사용자 |
| 지갑 주소 | 메타마스크 계정의 이더리움 주소 |
| 중복 투표 | 동일 지갑 주소가 같은 투표에 2회 이상 투표하는 행위 |
| 활성 후보자 | 삭제되지 않은 후보자. 스마트 컨트랙트의 `isActive=true` 상태 |
| 소프트 삭제 | 후보자 데이터를 실제 제거하지 않고 `isActive=false`로 비활성화하는 방식 |

### 1.5 문서 표기 기준

본 문서에서 사용자 화면에는 한글 상태명을 표시하고, 스마트 컨트랙트 내부 구현에는 영문 enum 상태값을 사용한다.

| 화면 표시 상태 | 스마트 컨트랙트 상태값 | 의미 |
|---|---|---|
| 준비 전 | `Ready` | 투표 시작 전 |
| 진행 중 | `Active` | 투표 진행 중 |
| 종료 | `Ended` | 투표 종료 |

본 문서에서 `후보자 2명 이상` 조건은 별도 설명이 없는 한 삭제되지 않은 활성 후보자 2명 이상을 의미한다.

## 2. 제품 요구사항 정의서(PRD)

### 2.1 제품 개요

본 제품은 웹 브라우저에서 접근 가능한 블록체인 기반 공개 투표 시스템이다. 사용자는 크롬 브라우저에 설치된 메타마스크를 통해 세폴리아 네트워크에 연결하고, 등록된 후보자 중 1명을 선택하여 투표할 수 있다.

투표 상태, 후보자 정보, 투표 여부, 득표수는 스마트 컨트랙트에서 관리한다. 후보자 사진 파일은 애플리케이션 서버에 저장하고, 스마트 컨트랙트에는 사진 URL 또는 파일 식별자를 저장한다.

### 2.2 제품 목표

- 웹 브라우저에서 쉽게 접근 가능한 투표 서비스 제공
- 메타마스크 기반 투표 참여 기능 제공
- 세폴리아 테스트넷 기반 투표 트랜잭션 처리
- 지갑 주소 기준 중복 투표 방지
- 투표 진행 중 득표 현황 공개
- 투표 종료 후 최종 결과 공개
- 관리자에게 후보자 관리 및 투표 운영 기능 제공

### 2.3 핵심 사용자

| 사용자 유형 | 설명 | 주요 행동 |
|---|---|---|
| 관리자 | 컨트랙트 배포자 주소를 사용하는 운영자 | 후보자 등록, 수정, 삭제, 투표 시작, 투표 종료, 결과 확인 |
| 유권자 | 투표에 참여하는 사용자 | 지갑 연결, 후보자 확인, 투표 참여, 결과 확인 |
| 일반 방문자 | 투표 현황을 조회하는 사용자 | 후보자 목록 확인, 현재 득표수 및 최종 결과 확인 |

### 2.4 주요 기능 요약

| 구분 | 기능 | 설명 |
|---|---|---|
| 지갑 연동 | 메타마스크 연결 | 사용자 지갑 주소 확인 |
| 네트워크 검증 | 세폴리아 확인 | 연결 네트워크가 세폴리아인지 확인 |
| 후보자 관리 | 후보자 등록 | 후보자 이름과 사진 등록 |
| 후보자 관리 | 후보자 수정 | 투표 시작 전 후보자 이름과 사진 수정 |
| 후보자 관리 | 후보자 삭제 | 투표 시작 전 후보자 소프트 삭제 |
| 투표 관리 | 투표 시작 | 활성 후보자 2명 이상일 때 관리자 수동 시작 |
| 투표 관리 | 투표 종료 | 관리자가 수동 종료 |
| 투표 참여 | 후보자 선택 후 투표 | 유권자는 후보자 1명에게 1회 투표 |
| 중복 방지 | 1지갑 1투표 | 동일 지갑 주소의 중복 투표 방지 |
| 현황 조회 | 진행 중 득표수 표시 | 공개 투표 정책에 따라 현재 득표수 표시 |
| 결과 조회 | 종료 후 최종 결과 표시 | 후보자별 전체 득표수와 최다 득표자 표시 |

### 2.5 제품 가정

- 사용자는 크롬 브라우저를 사용한다.
- 사용자는 메타마스크 확장 프로그램을 설치할 수 있다.
- 사용자는 세폴리아 테스트넷 ETH를 보유해야 투표 트랜잭션을 실행할 수 있다.
- 후보자 사진은 서버에 업로드되며, 스마트 컨트랙트에는 이미지 URL 또는 서버 파일 식별자만 저장한다.
- 관리자 권한은 스마트 컨트랙트 배포자 주소로 고정한다.
- 본 시스템은 단일 투표만 운영한다.

### 2.6 제외 범위

다음 기능은 본 문서의 기본 범위에 포함하지 않는다.

- 실명 인증
- 휴대폰 인증, 주민등록번호 인증 등 실제 사람 기준 본인 인증
- 투표권 사전 발급
- 비밀 투표
- 여러 개의 투표 동시 운영
- 예약 시작 및 예약 종료
- 관리자 주소 변경 기능
- 메타마스크 외 다른 지갑 연동
- 메인넷 배포
- 모바일 앱

### 2.7 성공 기준

- 사용자가 메타마스크로 세폴리아 네트워크에 연결할 수 있다.
- 관리자가 후보자 이름과 사진을 등록할 수 있다.
- 관리자가 투표 시작 전 후보자를 수정 또는 삭제할 수 있다.
- 활성 후보자 2명 이상일 때만 투표를 시작할 수 있다.
- 유권자가 후보자 1명에게 투표할 수 있다.
- 동일 지갑 주소는 중복 투표할 수 없다.
- 투표 진행 중 각 후보자의 현재 득표수가 공개된다.
- 투표 종료 후 최종 득표수와 최다 득표자를 확인할 수 있다.

## 3. 소프트웨어 요구사항 명세서(SRS)

### 3.1 시스템 구성

| 구성요소 | 설명 |
|---|---|
| 웹 프론트엔드 | 사용자 화면, 관리자 화면, 메타마스크 연동, 컨트랙트 호출 |
| 백엔드 서버 | 후보자 사진 업로드, 이미지 파일 저장, 이미지 URL 제공 |
| 스마트 컨트랙트 | 후보자 정보, 투표 상태, 투표 여부, 득표수 관리 |
| 파일 저장소 | 서버 로컬 스토리지 또는 서버가 관리하는 파일 저장 영역 |
| 블록체인 네트워크 | 이더리움 세폴리아 테스트넷 |

### 3.2 권한 요구사항

| 권한 | 설명 |
|---|---|
| 관리자 | 컨트랙트 배포자 주소. 후보자 등록, 수정, 삭제, 투표 시작, 투표 종료 가능 |
| 유권자 | 투표 진행 중 1회 투표 가능 |
| 일반 사용자 | 후보자 목록, 투표 상태, 득표수 조회 가능 |

### 3.3 투표 상태

| 상태 | 설명 |
|---|---|
| 준비 전 | 후보자 등록, 수정, 삭제 가능. 투표 불가 |
| 진행 중 | 투표 가능. 후보자 등록, 수정, 삭제 불가 |
| 종료 | 투표 불가. 최종 결과 조회 가능 |

### 3.4 기능 요구사항

#### FR-001 메타마스크 연결

- 사용자는 웹 화면에서 메타마스크 연결 버튼을 클릭할 수 있어야 한다.
- 시스템은 사용자의 지갑 주소를 가져올 수 있어야 한다.
- 지갑 연결 성공 시 화면에 지갑 주소를 축약 형태로 표시해야 한다.
- 메타마스크가 설치되어 있지 않은 경우 설치 안내 메시지를 표시해야 한다.

#### FR-002 세폴리아 네트워크 검증

- 시스템은 사용자가 연결한 네트워크가 세폴리아인지 확인해야 한다.
- 세폴리아가 아닌 경우 사용자에게 네트워크 전환을 안내해야 한다.
- 가능하다면 메타마스크 네트워크 전환 요청을 제공해야 한다.

#### FR-003 후보자 등록

- 관리자는 투표 시작 전 후보자를 등록할 수 있어야 한다.
- 후보자 이름은 필수값이어야 한다.
- 후보자 사진은 필수값이어야 한다.
- 후보자 사진은 서버에 업로드되어 저장되어야 한다.
- 스마트 컨트랙트에는 후보자 사진 파일 자체가 아니라 서버 이미지 URL 또는 파일 식별자를 저장해야 한다.
- 투표 진행 중 또는 종료 후에는 후보자 등록이 불가능해야 한다.

#### FR-004 후보자 수정

- 관리자는 투표 시작 전 후보자 정보를 수정할 수 있어야 한다.
- 수정 가능한 항목은 후보자 이름과 후보자 사진이다.
- 후보자 사진 변경 시 새 이미지는 서버에 업로드되어야 한다.
- 스마트 컨트랙트에는 변경된 이미지 URL 또는 파일 식별자가 반영되어야 한다.
- 투표 진행 중 또는 종료 후에는 후보자 수정이 불가능해야 한다.

#### FR-005 후보자 삭제

- 관리자는 투표 시작 전 후보자를 삭제할 수 있어야 한다.
- 삭제된 후보자는 후보자 목록에서 제외되어야 한다.
- 투표 시작 후에는 후보자를 삭제할 수 없어야 한다.
- 후보자 삭제 후 남은 활성 후보자가 2명 미만이면 투표 시작이 불가능해야 한다.

#### FR-006 후보자 목록 조회

- 사용자는 등록된 후보자 목록을 확인할 수 있어야 한다.
- 후보자 목록에는 후보자 ID, 후보자 이름, 후보자 사진, 득표수가 표시되어야 한다.
- 투표 진행 중에는 현재 득표수가 표시되어야 한다.
- 투표 종료 후에는 최종 득표수가 표시되어야 한다.

#### FR-007 투표 시작

- 관리자는 활성 후보자가 최소 2명 이상인 경우에만 투표를 시작할 수 있어야 한다.
- 투표 시작은 관리자가 수동으로 실행한다.
- 투표 시작 후 투표 상태는 `진행 중`으로 변경되어야 한다.
- 투표 시작 후 후보자 등록, 수정, 삭제는 불가능해야 한다.

#### FR-008 투표 참여

- 유권자는 투표 진행 중 후보자 1명을 선택하여 투표할 수 있어야 한다.
- 투표는 메타마스크 트랜잭션 서명을 통해 실행되어야 한다.
- 트랜잭션 성공 후 선택한 후보자의 득표수가 증가해야 한다.
- 투표 준비 전 또는 종료 후에는 투표할 수 없어야 한다.

#### FR-009 중복 투표 방지

- 시스템은 동일 지갑 주소가 같은 투표에 2회 이상 투표하지 못하도록 해야 한다.
- 중복 투표 방지는 스마트 컨트랙트 레벨에서 처리해야 한다.
- 중복 투표 시도 시 스마트 컨트랙트는 트랜잭션을 거부해야 한다.
- 프론트엔드는 이미 투표한 사용자에게 투표 완료 상태를 표시해야 한다.

#### FR-010 투표 종료

- 관리자는 진행 중인 투표를 수동으로 종료할 수 있어야 한다.
- 투표 종료 후 상태는 `종료`로 변경되어야 한다.
- 종료 후에는 추가 투표가 불가능해야 한다.

#### FR-011 진행 중 득표수 공개

- 투표 진행 중 사용자는 각 후보자의 현재 득표수를 확인할 수 있어야 한다.
- 득표수는 공개 투표 정책에 따라 모든 사용자에게 표시되어야 한다.
- 득표수는 스마트 컨트랙트의 최신 상태를 기준으로 표시해야 한다.
- 트랜잭션 반영 후 화면은 득표수를 갱신해야 한다.

#### FR-012 최종 결과 조회

- 투표 종료 후 사용자는 전체 후보자의 최종 득표수를 확인할 수 있어야 한다.
- 최다 득표 후보자를 화면에 명확히 표시해야 한다.
- 동점자가 있는 경우 동점 상태를 표시해야 한다.

#### FR-013 관리자 식별

- 시스템은 연결된 지갑 주소가 컨트랙트 배포자 주소인지 확인해야 한다.
- 컨트랙트 배포자 주소만 관리자 기능을 사용할 수 있어야 한다.
- 관리자 주소 변경 기능은 제공하지 않는다.

#### FR-014 서버 이미지 업로드

- 관리자는 후보자 사진 파일을 서버에 업로드할 수 있어야 한다.
- 서버는 업로드된 이미지 파일의 URL 또는 식별자를 반환해야 한다.
- 허용 파일 형식은 JPG, PNG, WebP를 권장한다.
- 파일 크기 제한을 두어야 하며, 기본 권장값은 5MB 이하이다.
- 이미지 업로드 실패 시 후보자 등록 또는 수정 트랜잭션을 실행하지 않아야 한다.

#### FR-015 오류 처리

- 시스템은 다음 상황에 대해 사용자에게 명확한 메시지를 표시해야 한다.
  - 메타마스크 미설치
  - 지갑 연결 거부
  - 세폴리아 네트워크 미연결
  - 트랜잭션 거부
  - 트랜잭션 실패
  - 이미 투표한 사용자
  - 활성 후보자 2명 미만 상태에서 투표 시작 시도
  - 후보자 이미지 업로드 실패
  - 관리자 권한 없음

### 3.5 데이터 요구사항

#### 후보자 데이터

| 필드 | 설명 | 필수 여부 | 저장 위치 |
|---|---|---|---|
| candidateId | 후보자 고유 ID | 필수 | 스마트 컨트랙트 |
| name | 후보자 이름 | 필수 | 스마트 컨트랙트 |
| imageUrl | 후보자 사진 URL 또는 파일 식별자 | 필수 | 스마트 컨트랙트 |
| voteCount | 득표수 | 필수 | 스마트 컨트랙트 |
| isActive | 삭제 여부 구분 | 필수 | 스마트 컨트랙트 |

#### 이미지 파일 데이터

| 필드 | 설명 | 필수 여부 | 저장 위치 |
|---|---|---|---|
| fileName | 저장 파일명 | 필수 | 서버 |
| originalName | 원본 파일명 | 선택 | 서버 |
| mimeType | 파일 형식 | 필수 | 서버 |
| size | 파일 크기 | 필수 | 서버 |
| url | 접근 가능한 이미지 URL | 필수 | 서버 |

#### 투표 상태 데이터

| 필드 | 설명 | 필수 여부 | 저장 위치 |
|---|---|---|---|
| status | 준비 전, 진행 중, 종료 | 필수 | 스마트 컨트랙트 |
| startedAt | 투표 시작 시각 | 선택 | 스마트 컨트랙트 |
| endedAt | 투표 종료 시각 | 선택 | 스마트 컨트랙트 |
| owner | 컨트랙트 배포자 주소 | 필수 | 스마트 컨트랙트 |

스마트 컨트랙트 내부 상태값은 `Ready`, `Active`, `Ended`를 사용하며, 프론트엔드는 이를 각각 `준비 전`, `진행 중`, `종료`로 표시한다.

#### 투표자 데이터

| 필드 | 설명 | 필수 여부 | 저장 위치 |
|---|---|---|---|
| voterAddress | 투표자 지갑 주소 | 필수 | 스마트 컨트랙트 |
| hasVoted | 투표 여부 | 필수 | 스마트 컨트랙트 |
| votedCandidateId | 선택 후보자 ID | 선택 | 스마트 컨트랙트 |

### 3.6 스마트 컨트랙트 요구사항

스마트 컨트랙트는 다음 기능을 제공해야 한다.

| 함수 | 설명 | 권한 |
|---|---|---|
| addCandidate(name, imageUrl) | 후보자 등록 | 관리자 |
| updateCandidate(candidateId, name, imageUrl) | 후보자 수정 | 관리자 |
| deleteCandidate(candidateId) | 후보자 삭제 | 관리자 |
| startVoting() | 투표 시작 | 관리자 |
| endVoting() | 투표 종료 | 관리자 |
| vote(candidateId) | 후보자에게 투표 | 유권자 |
| getCandidates() | 활성 후보자 목록 조회 | 전체 |
| getVoteStatus() | 투표 상태 조회 | 전체 |
| hasVoted(address) | 특정 주소의 투표 여부 조회 | 전체 |
| owner() | 관리자 주소 조회 | 전체 |

스마트 컨트랙트는 다음 조건을 보장해야 한다.

- 배포자 주소를 관리자 주소로 설정해야 한다.
- 관리자 주소 변경 기능은 제공하지 않는다.
- 관리자만 후보자 등록, 수정, 삭제, 투표 시작, 투표 종료를 할 수 있어야 한다.
- 후보자는 투표 시작 전까지만 등록, 수정, 삭제 가능해야 한다.
- 투표 시작 시 활성 후보자가 최소 2명 이상이어야 한다.
- 투표는 진행 중 상태에서만 가능해야 한다.
- 삭제된 후보자에게는 투표할 수 없어야 한다.
- 동일 지갑 주소는 1회만 투표할 수 있어야 한다.
- 투표 종료 후에는 투표할 수 없어야 한다.

#### 스마트 컨트랙트 상태 모델

구현 시 투표 상태는 다음 enum 또는 동일한 의미의 상태값으로 관리한다.

| 상태값 | 의미 | 허용 기능 |
|---|---|---|
| `Ready` | 투표 준비 전 | 후보자 등록, 수정, 삭제, 투표 시작 |
| `Active` | 투표 진행 중 | 투표, 득표수 조회, 투표 종료 |
| `Ended` | 투표 종료 | 최종 결과 조회 |

#### 후보자 삭제 정책

후보자 삭제는 실제 배열 원소 삭제가 아니라 소프트 삭제로 구현한다.

- 후보자 구조체에는 `isActive` 필드를 포함한다.
- 삭제 시 `isActive=false`로 변경한다.
- 후보자 ID는 한 번 발급되면 재사용하지 않는다.
- 투표 시작 조건은 전체 후보자 수가 아니라 활성 후보자 수 기준으로 판단한다.
- 삭제된 후보자에게 투표 요청이 들어오면 스마트 컨트랙트에서 거부한다.

#### 스마트 컨트랙트 이벤트

프론트엔드 갱신과 블록체인 로그 추적을 위해 다음 이벤트를 발생시켜야 한다.

| 이벤트 | 발생 시점 | 포함 권장 데이터 |
|---|---|---|
| `CandidateAdded` | 후보자 등록 성공 | candidateId, name, imageUrl |
| `CandidateUpdated` | 후보자 수정 성공 | candidateId, name, imageUrl |
| `CandidateDeleted` | 후보자 삭제 성공 | candidateId |
| `VotingStarted` | 투표 시작 성공 | startedAt |
| `VotingEnded` | 투표 종료 성공 | endedAt |
| `VoteCast` | 투표 성공 | voter, candidateId |

#### 스마트 컨트랙트 검증 조건

각 함수는 다음 조건을 만족하지 않으면 revert 처리해야 한다.

| 함수 | 필수 검증 |
|---|---|
| `addCandidate` | 관리자만 호출 가능, 상태가 `Ready`, 이름 비어있지 않음, imageUrl 비어있지 않음 |
| `updateCandidate` | 관리자만 호출 가능, 상태가 `Ready`, 후보자 존재, 후보자 활성 상태, 이름/imageUrl 비어있지 않음 |
| `deleteCandidate` | 관리자만 호출 가능, 상태가 `Ready`, 후보자 존재, 후보자 활성 상태 |
| `startVoting` | 관리자만 호출 가능, 상태가 `Ready`, 활성 후보자 2명 이상 |
| `endVoting` | 관리자만 호출 가능, 상태가 `Active` |
| `vote` | 상태가 `Active`, 후보자 존재, 후보자 활성 상태, 호출자 미투표 상태 |

#### 후보자 수 제한

1차 버전에서는 가스 비용과 화면 표시 복잡도를 줄이기 위해 최대 후보자 수를 50명으로 제한한다.

- 최대 후보자 수: 50명
- 최대 후보자 수를 초과하면 후보자 등록을 거부한다.
- 후보자 수 제한값은 컨트랙트 상수로 관리한다.

### 3.7 백엔드 서버 요구사항

- 후보자 사진 업로드 API를 제공해야 한다.
- 업로드된 이미지를 서버에 저장해야 한다.
- 이미지 접근 URL을 프론트엔드에 반환해야 한다.
- 허용되지 않은 파일 형식은 거부해야 한다.
- 파일 크기 제한을 초과한 업로드는 거부해야 한다.
- 이미지 파일명은 충돌을 피할 수 있도록 고유하게 생성해야 한다.
- 후보자 삭제 또는 사진 변경 시 기존 이미지 파일 정리 정책을 정의해야 한다.

#### 백엔드 API 요구사항

1차 버전에서 필요한 백엔드 API는 다음과 같다.

| Method | Path | 설명 | 인증 |
|---|---|---|---|
| `POST` | `/api/auth/nonce` | 관리자 지갑 서명용 nonce 발급 | 없음 |
| `POST` | `/api/auth/verify` | 관리자 지갑 서명 검증 및 업로드 토큰 발급 | 지갑 서명 |
| `POST` | `/api/candidate-images` | 후보자 이미지 업로드 | 관리자 업로드 토큰 |
| `GET` | `/uploads/{fileName}` | 업로드 이미지 조회 | 없음 |

#### 이미지 업로드 인증 방식

서버 이미지 업로드는 관리자만 가능해야 한다. 권장 인증 흐름은 다음과 같다.

1. 프론트엔드가 서버에 nonce를 요청한다.
2. 관리자가 메타마스크로 nonce 메시지에 서명한다.
3. 프론트엔드가 서명값과 지갑 주소를 서버에 전달한다.
4. 서버가 서명 복구 주소와 서버 환경 변수 `ADMIN_ADDRESS`를 비교한다.
5. 검증 성공 시 짧은 만료 시간을 가진 업로드 토큰을 발급한다.
6. 이미지 업로드 API는 해당 토큰이 유효할 때만 파일 업로드를 허용한다.

`ADMIN_ADDRESS`는 세폴리아에 배포된 투표 컨트랙트의 `owner()` 값과 동일해야 한다. 배포 후 서버 설정값과 컨트랙트 owner 값이 다르면 관리자 이미지 업로드를 허용하지 않는다.

#### 이미지 파일 검증 정책

| 항목 | 기준 |
|---|---|
| 허용 확장자 | `.jpg`, `.jpeg`, `.png`, `.webp` |
| 허용 MIME 타입 | `image/jpeg`, `image/png`, `image/webp` |
| 최대 파일 크기 | 5MB |
| 파일명 | UUID 또는 충돌 가능성이 낮은 서버 생성 파일명 |
| 공개 URL | HTTPS 환경에서는 HTTPS URL 제공 |

#### 이미지 정합성 처리

이미지 업로드와 스마트 컨트랙트 트랜잭션은 원자적으로 묶이지 않는다. 따라서 다음 정책을 적용한다.

| 상황 | 처리 정책 |
|---|---|
| 이미지 업로드 실패 | 후보자 등록/수정 트랜잭션을 실행하지 않는다 |
| 이미지 업로드 성공 후 트랜잭션 실패 | 업로드 파일을 미사용 상태로 기록하고 정리 대상에 포함한다 |
| 후보자 사진 수정 성공 | 기존 이미지는 즉시 삭제하지 않고 보관 후 정리한다 |
| 후보자 삭제 성공 | 이미지 파일은 즉시 삭제하지 않고 보관 후 정리한다 |

1차 버전에서는 파일 유실로 후보자 사진이 깨지는 문제를 피하기 위해 기존 이미지를 즉시 삭제하지 않는 것을 권장한다.

### 3.8 프론트엔드 요구사항

- 크롬 브라우저에서 정상 동작해야 한다.
- 메타마스크 브라우저 확장 프로그램과 연동되어야 한다.
- 반응형 웹 UI를 제공해야 한다.
- 지갑 연결 전후 상태를 구분해 표시해야 한다.
- 관리자와 일반 사용자의 화면 권한을 구분해야 한다.
- 트랜잭션 처리 중 로딩 상태를 표시해야 한다.
- 이미지 업로드 중 로딩 상태를 표시해야 한다.
- 트랜잭션 성공 및 실패 결과를 사용자에게 알려야 한다.
- 공개 투표 정책에 따라 투표 진행 중 득표수를 표시해야 한다.

#### 프론트엔드 트랜잭션 처리 흐름

후보자 등록/수정/삭제, 투표 시작/종료, 투표 참여는 다음 공통 흐름으로 처리한다.

1. 지갑 연결 여부를 확인한다.
2. 세폴리아 Chain ID `0xaa36a7` 여부를 확인한다.
3. 필요한 경우 관리자 주소 여부 또는 투표 가능 여부를 사전 조회한다.
4. 사용자 액션에 따라 메타마스크 트랜잭션 요청을 실행한다.
5. 사용자가 트랜잭션을 거부하면 오류 메시지를 표시한다.
6. 트랜잭션이 제출되면 처리 중 상태를 표시한다.
7. 트랜잭션 receipt 확인 후 후보자 목록, 투표 상태, 득표수를 다시 조회한다.

#### 프론트엔드 실시간 갱신 정책

1차 버전에서는 다음 중 하나의 방식으로 화면을 갱신한다.

- 트랜잭션 완료 후 수동 재조회
- 일정 주기 polling으로 후보자 목록 및 득표수 재조회
- 컨트랙트 이벤트 구독 기반 갱신

구현 난이도와 안정성을 고려하면 1차 버전은 `트랜잭션 완료 후 재조회 + 일정 주기 polling` 방식을 권장한다.

#### 필수 환경 변수

프론트엔드와 백엔드는 다음 설정값을 환경 변수 또는 배포 설정으로 관리해야 한다.

| 변수 | 설명 |
|---|---|
| `SEPOLIA_CHAIN_ID` | `0xaa36a7` |
| `CONTRACT_ADDRESS` | 세폴리아에 배포된 투표 컨트랙트 주소 |
| `RPC_URL` | 세폴리아 RPC URL |
| `UPLOAD_BASE_URL` | 후보자 이미지 공개 접근 URL |
| `ADMIN_ADDRESS` | 컨트랙트 배포자 주소. 서버 검증용이며 컨트랙트 `owner()` 값과 일치해야 함 |
| `UPLOAD_TOKEN_SECRET` | 업로드 토큰 서명용 서버 비밀값 |

### 3.9 비기능 요구사항

#### NFR-001 보안

- 중복 투표 방지는 반드시 스마트 컨트랙트 레벨에서 처리해야 한다.
- 관리자 권한 검증은 반드시 스마트 컨트랙트 레벨에서 처리해야 한다.
- 프론트엔드의 버튼 비활성화만으로 권한을 제어해서는 안 된다.
- 이미지 업로드 시 파일 형식과 파일 크기를 검증해야 한다.

#### NFR-002 사용성

- 사용자는 현재 지갑 연결 상태를 쉽게 확인할 수 있어야 한다.
- 사용자는 현재 세폴리아 네트워크 연결 상태를 쉽게 확인할 수 있어야 한다.
- 사용자는 현재 투표 가능 여부를 명확히 알 수 있어야 한다.
- 트랜잭션 대기 중에는 중복 클릭을 방지해야 한다.

#### NFR-003 호환성

- 최신 버전의 크롬 브라우저를 지원해야 한다.
- 최신 버전의 메타마스크 확장 프로그램을 지원해야 한다.

#### NFR-004 성능

- 후보자 목록 조회 화면은 일반적인 후보자 수 기준으로 3초 이내에 렌더링되어야 한다.
- 이미지 업로드는 일반적인 네트워크 환경에서 사용자가 진행 상태를 인지할 수 있어야 한다.
- 트랜잭션 완료 시간은 세폴리아 네트워크 상태에 의존하므로 별도 보장하지 않는다.

#### NFR-005 가용성

- 웹 애플리케이션은 일반 사용자가 URL로 접근 가능해야 한다.
- 스마트 컨트랙트가 배포된 세폴리아 네트워크와 정상 연결되어야 한다.
- 서버에 저장된 후보자 사진은 후보자 목록 화면에서 접근 가능해야 한다.

### 3.10 제약사항

- 세폴리아 네트워크는 테스트넷이므로 실제 법적 효력이 필요한 선거에는 사용할 수 없다.
- 지갑 주소 기준 1인 1투표는 실제 사람 기준 1인 1투표를 보장하지 않는다.
- 사용자가 여러 지갑 주소를 보유하면 여러 번 투표할 가능성이 있다.
- 진행 중 득표수가 공개되므로 비밀 투표나 비공개 개표 요구사항에는 적합하지 않다.

## 4. 화면 설계서

### 4.1 공통 레이아웃

#### 상단 영역

| 요소 | 설명 |
|---|---|
| 서비스명 | 투표 시스템 이름 표시 |
| 네트워크 상태 | 세폴리아 연결 여부 표시 |
| 지갑 연결 버튼 | 미연결 시 `지갑 연결`, 연결 시 축약 주소 표시 |
| 관리자 표시 | 컨트랙트 배포자 주소로 접속한 경우 관리자 배지 표시 |

#### 본문 영역

- 투표 상태 안내
- 후보자 목록
- 후보자별 현재 또는 최종 득표수
- 투표 버튼 또는 결과 표시
- 관리자 기능 영역

#### 알림 영역

- 이미지 업로드 진행 상태
- 트랜잭션 진행 상태
- 오류 메시지
- 성공 메시지

### 4.2 메인 화면

#### 화면 목적

사용자가 투표 상태, 후보자 목록, 득표수, 지갑 연결 상태를 확인하고 투표에 참여할 수 있는 기본 화면이다.

#### 주요 구성요소

| 영역 | 항목 | 설명 |
|---|---|---|
| 헤더 | 서비스명 | 예: Sepolia Voting |
| 헤더 | 지갑 연결 버튼 | 미연결 시 `지갑 연결`, 연결 시 축약 주소 표시 |
| 헤더 | 네트워크 상태 | `Sepolia 연결됨` 또는 `네트워크 변경 필요` |
| 상태 영역 | 투표 상태 | 준비 전, 진행 중, 종료 표시 |
| 상태 영역 | 투표 안내 | 현재 사용자가 투표 가능한지 표시 |
| 후보자 영역 | 후보자 카드 | 후보자 사진, 이름, 득표수, 투표 버튼 표시 |

#### 후보자 카드 구성

| 항목 | 설명 |
|---|---|
| 후보자 사진 | 서버에 저장된 후보자 이미지 표시 |
| 후보자 이름 | 후보자 이름 표시 |
| 득표수 | 진행 중 현재 득표수 또는 종료 후 최종 득표수 표시 |
| 투표 버튼 | 진행 중이고 미투표자인 경우 활성화 |

#### 상태별 화면 동작

| 투표 상태 | 화면 동작 |
|---|---|
| 준비 전 | 후보자 목록과 투표 준비 중 메시지 표시, 투표 버튼 비활성화 |
| 진행 중 | 후보자별 현재 득표수 공개, 미투표자는 투표 가능 |
| 종료 | 후보자별 최종 득표수 표시, 최다 득표자 또는 동점 결과 표시 |

### 4.3 지갑 연결 화면 흐름

1. 사용자가 `지갑 연결` 버튼을 클릭한다.
2. 메타마스크 연결 요청 팝업이 열린다.
3. 사용자가 계정 연결을 승인한다.
4. 시스템이 연결된 주소와 네트워크를 확인한다.
5. 세폴리아가 아닌 경우 네트워크 전환 안내를 표시한다.
6. 세폴리아 연결 완료 후 투표 기능을 사용할 수 있다.

| 상황 | 화면 메시지 |
|---|---|
| 메타마스크 미설치 | 메타마스크 설치가 필요합니다. |
| 연결 거부 | 지갑 연결이 취소되었습니다. |
| 네트워크 불일치 | 세폴리아 네트워크로 전환해 주세요. |

### 4.4 관리자 화면

#### 화면 목적

관리자가 후보자를 등록, 수정, 삭제하고 투표를 시작하거나 종료할 수 있도록 한다.

#### 접근 조건

- 연결된 지갑 주소가 스마트 컨트랙트 배포자 주소와 일치해야 한다.

#### 주요 구성요소

| 영역 | 항목 | 설명 |
|---|---|---|
| 후보자 등록 | 후보자 이름 입력 | 후보자 이름 입력 필드 |
| 후보자 등록 | 후보자 사진 업로드 | 서버로 이미지 업로드 |
| 후보자 등록 | 등록 버튼 | 이미지 업로드 후 후보자 등록 트랜잭션 실행 |
| 후보자 관리 | 수정 버튼 | 투표 시작 전 후보자 정보 수정 |
| 후보자 관리 | 삭제 버튼 | 투표 시작 전 후보자 삭제 |
| 투표 관리 | 투표 시작 버튼 | 활성 후보자 2명 이상일 때 활성화 |
| 투표 관리 | 투표 종료 버튼 | 투표 진행 중일 때 활성화 |

#### 관리자 화면 상태

| 상태 | 후보자 등록 | 후보자 수정 | 후보자 삭제 | 투표 시작 | 투표 종료 |
|---|---|---|---|---|---|
| 준비 전, 활성 후보자 2명 미만 | 가능 | 가능 | 가능 | 비활성화 | 비활성화 |
| 준비 전, 활성 후보자 2명 이상 | 가능 | 가능 | 가능 | 활성화 | 비활성화 |
| 진행 중 | 불가능 | 불가능 | 불가능 | 비활성화 | 활성화 |
| 종료 | 불가능 | 불가능 | 불가능 | 비활성화 | 비활성화 |

### 4.5 후보자 등록/수정 화면

#### 화면 목적

관리자가 후보자 이름과 사진을 입력하거나 변경할 수 있도록 한다.

| 항목 | 설명 |
|---|---|
| 후보자 이름 | 필수 입력 |
| 후보자 사진 | 등록 시 필수 업로드, 수정 시 기존 사진 유지 또는 새 사진 업로드 |
| 이미지 미리보기 | 업로드 또는 선택한 이미지 표시 |
| 저장 버튼 | 등록 또는 수정 실행 |
| 취소 버튼 | 입력 내용 초기화 또는 편집 취소 |

#### 처리 흐름

1. 관리자가 후보자 이름을 입력한다.
2. 후보자 등록이거나 사진을 변경하는 수정인 경우 후보자 사진 파일을 선택한다.
3. 시스템이 파일 형식과 크기를 검증한다.
4. 서버 업로드가 필요한 경우 관리자 지갑 서명 검증 후 이미지를 업로드한다.
5. 서버가 이미지 URL 또는 파일 식별자를 반환한다.
6. 프론트엔드가 스마트 컨트랙트에 후보자 등록 또는 수정 트랜잭션을 요청한다.
7. 메타마스크에서 트랜잭션을 승인한다.
8. 후보자 목록이 갱신된다.

### 4.6 투표 진행 화면

#### 화면 목적

유권자가 후보자를 확인하고 한 명에게 투표할 수 있도록 한다. 공개 투표 정책에 따라 진행 중 득표수를 함께 표시한다.

#### 주요 동작

- 후보자 카드마다 현재 득표수와 투표 버튼을 표시한다.
- 사용자가 이미 투표한 경우 모든 투표 버튼을 비활성화한다.
- 투표 완료 후 `투표가 완료되었습니다.` 메시지를 표시한다.
- 트랜잭션 처리 중에는 해당 버튼에 로딩 상태를 표시한다.

#### 투표 버튼 활성화 조건

| 조건 | 필요 여부 |
|---|---|
| 지갑 연결됨 | 필수 |
| 세폴리아 네트워크 연결됨 | 필수 |
| 투표 상태가 진행 중 | 필수 |
| 사용자가 아직 투표하지 않음 | 필수 |
| 후보자가 삭제되지 않은 활성 후보자임 | 필수 |

### 4.7 결과 화면

#### 화면 목적

투표 종료 후 최종 득표수와 결과를 확인할 수 있도록 한다.

| 항목 | 설명 |
|---|---|
| 결과 상태 | `투표가 종료되었습니다.` 표시 |
| 최종 득표수 | 후보자별 전체 득표수 표시 |
| 1위 후보 | 최다 득표 후보 강조 |
| 동점 표시 | 최다 득표자가 여러 명이면 동점으로 표시 |

### 4.8 화면 와이어프레임

#### 메인 화면

```text
+------------------------------------------------------+
| Sepolia Voting                  [Sepolia] [0x12...ab] |
+------------------------------------------------------+
| 투표 상태: 진행 중                                   |
| 안내: 후보자를 선택해 투표할 수 있습니다.             |
+------------------------------------------------------+
| [후보자 사진]        [후보자 사진]                    |
| 후보자 A             후보자 B                         |
| 현재 득표수: 10      현재 득표수: 8                   |
| [투표하기]           [투표하기]                       |
+------------------------------------------------------+
```

#### 관리자 화면

```text
+------------------------------------------------------+
| 관리자 패널                                           |
+------------------------------------------------------+
| 후보자 이름: [____________________]                   |
| 후보자 사진: [파일 선택] [미리보기]                   |
| [후보자 등록]                                         |
+------------------------------------------------------+
| 활성 후보자 수: 2명                                   |
| 후보자 A [수정] [삭제]                                |
| 후보자 B [수정] [삭제]                                |
| [투표 시작] [투표 종료]                               |
+------------------------------------------------------+
```

#### 결과 화면

```text
+------------------------------------------------------+
| 투표 상태: 종료                                      |
| 최종 결과                                             |
+------------------------------------------------------+
| 1위: 후보자 A                                         |
| 후보자 A - 15표                                      |
| 후보자 B - 11표                                      |
+------------------------------------------------------+
```

## 5. 사용자 시나리오

### 5.1 관리자 후보자 등록 시나리오

1. 관리자가 웹사이트에 접속한다.
2. 메타마스크 지갑을 연결한다.
3. 시스템이 컨트랙트 배포자 주소 여부를 확인한다.
4. 관리자가 후보자 이름과 사진을 입력한다.
5. 관리자가 후보자 등록 버튼을 클릭한다.
6. 관리자 지갑 서명 검증 후 후보자 사진이 서버에 업로드된다.
7. 메타마스크에서 후보자 등록 트랜잭션을 승인한다.
8. 후보자가 목록에 추가된다.

### 5.2 관리자 후보자 수정 시나리오

1. 관리자가 준비 전 상태에서 후보자 수정 버튼을 클릭한다.
2. 후보자 이름 또는 사진을 변경한다.
3. 사진을 변경한 경우 관리자 지갑 서명 검증 후 새 이미지가 서버에 업로드된다.
4. 메타마스크에서 수정 트랜잭션을 승인한다.
5. 후보자 목록에 변경 내용이 반영된다.

### 5.3 관리자 후보자 삭제 시나리오

1. 관리자가 준비 전 상태에서 후보자 삭제 버튼을 클릭한다.
2. 시스템이 삭제 확인 메시지를 표시한다.
3. 관리자가 삭제를 확인한다.
4. 메타마스크에서 삭제 트랜잭션을 승인한다.
5. 후보자는 `isActive=false` 상태로 변경되고 목록에서 제외된다.

### 5.4 관리자 투표 시작 시나리오

1. 관리자가 활성 후보자 2명 이상이 등록된 것을 확인한다.
2. 투표 시작 버튼을 클릭한다.
3. 메타마스크에서 트랜잭션을 승인한다.
4. 투표 상태가 진행 중으로 변경된다.
5. 유권자가 투표할 수 있게 된다.

### 5.5 유권자 투표 시나리오

1. 유권자가 웹사이트에 접속한다.
2. 메타마스크 지갑을 연결한다.
3. 세폴리아 네트워크 연결 상태를 확인한다.
4. 후보자 목록과 현재 득표수를 확인한다.
5. 원하는 후보자의 투표 버튼을 클릭한다.
6. 메타마스크에서 트랜잭션을 승인한다.
7. 트랜잭션 성공 후 투표 완료 메시지를 확인한다.
8. 해당 후보자의 득표수가 증가한다.

### 5.6 중복 투표 방지 시나리오

1. 이미 투표한 유권자가 다시 투표하려고 한다.
2. 프론트엔드는 투표 버튼을 비활성화한다.
3. 직접 컨트랙트 호출을 시도하더라도 스마트 컨트랙트가 중복 투표를 거부한다.

### 5.7 투표 종료 및 결과 확인 시나리오

1. 관리자가 투표 종료 버튼을 클릭한다.
2. 메타마스크에서 트랜잭션을 승인한다.
3. 투표 상태가 종료로 변경된다.
4. 모든 투표 버튼이 비활성화된다.
5. 사용자는 최종 득표수와 1위 후보를 확인한다.

## 6. 테스트 기준

### 6.1 기능 테스트

| 테스트 항목 | 기대 결과 |
|---|---|
| 메타마스크 연결 | 지갑 주소가 정상 표시된다 |
| 세폴리아 외 네트워크 연결 | 네트워크 전환 안내가 표시된다 |
| 후보자 이미지 업로드 | 서버에 이미지가 저장되고 URL이 반환된다 |
| 후보자 등록 | 후보자가 목록에 추가된다 |
| 투표 시작 전 후보자 수정 | 후보자 정보가 변경된다 |
| 투표 시작 전 후보자 삭제 | 후보자가 목록에서 제외된다 |
| 활성 후보자 1명 상태에서 투표 시작 | 투표 시작이 실패한다 |
| 활성 후보자 2명 상태에서 투표 시작 | 투표가 시작된다 |
| 투표 진행 중 후보자 등록 | 후보자 등록이 실패한다 |
| 투표 진행 중 후보자 수정 | 후보자 수정이 실패한다 |
| 투표 진행 중 후보자 삭제 | 후보자 삭제가 실패한다 |
| 유권자 투표 | 선택 후보자의 득표수가 증가한다 |
| 동일 주소 재투표 | 트랜잭션이 거부된다 |
| 투표 종료 후 투표 | 트랜잭션이 거부된다 |
| 투표 진행 중 득표수 조회 | 현재 득표수가 표시된다 |
| 투표 종료 후 결과 조회 | 최종 득표수가 표시된다 |

### 6.2 화면 테스트

| 테스트 항목 | 기대 결과 |
|---|---|
| 지갑 미연결 상태 | 지갑 연결 버튼이 표시된다 |
| 지갑 연결 상태 | 축약 주소가 표시된다 |
| 관리자 접속 | 관리자 패널이 표시된다 |
| 일반 사용자 접속 | 관리자 패널이 표시되지 않는다 |
| 준비 전 상태 | 후보자 관리 기능이 활성화된다 |
| 투표 진행 중 | 투표 버튼과 현재 득표수가 표시된다 |
| 투표 종료 | 결과 화면이 표시된다 |

### 6.3 스마트 컨트랙트 테스트

| 테스트 항목 | 기대 결과 |
|---|---|
| 관리자 외 후보자 등록 | 실패 |
| 관리자 후보자 등록 | 성공 |
| 관리자 후보자 수정 | 성공 |
| 관리자 후보자 삭제 | 성공 |
| 투표 시작 후 후보자 수정 | 실패 |
| 투표 시작 후 후보자 삭제 | 실패 |
| 활성 후보자 2명 미만 투표 시작 | 실패 |
| 활성 후보자 2명 이상 투표 시작 | 성공 |
| 진행 중 투표 | 성공 |
| 동일 주소 중복 투표 | 실패 |
| 종료 후 투표 | 실패 |
| 관리자 외 투표 종료 | 실패 |

## 7. 구현 결정사항 요약

| 항목 | 결정 |
|---|---|
| 후보자 사진 | 서버 저장 |
| 관리자 | 컨트랙트 배포자 주소 고정 |
| 투표 시작/종료 | 관리자 수동 처리 |
| 중복 투표 기준 | 지갑 주소 기준 |
| 후보자 수정/삭제 | 투표 시작 전 허용 |
| 투표 개수 | 단일 투표 |
| 득표수 공개 | 진행 중 및 종료 후 모두 공개 |

## 8. 승인 기준

본 시스템은 다음 조건을 만족하면 1차 버전 개발 완료로 본다.

- 세폴리아 네트워크에 스마트 컨트랙트가 배포되어 있다.
- 웹 브라우저에서 서비스에 접속할 수 있다.
- 메타마스크로 지갑 연결이 가능하다.
- 후보자 이름과 사진을 등록할 수 있다.
- 후보자 사진은 서버에 저장된다.
- 투표 시작 전 후보자를 수정 또는 삭제할 수 있다.
- 활성 후보자 2명 이상일 때 관리자가 수동으로 투표를 시작할 수 있다.
- 관리자가 수동으로 투표를 종료할 수 있다.
- 유권자가 후보자 1명에게 투표할 수 있다.
- 동일 지갑 주소는 중복 투표할 수 없다.
- 투표 진행 중 현재 득표수를 볼 수 있다.
- 투표 종료 후 최종 득표수를 볼 수 있다.
- 관리자 외 사용자는 관리자 기능을 실행할 수 없다.
- 후보자 이미지 업로드 API는 관리자 권한 검증을 수행한다.
- 후보자 삭제는 `isActive=false` 방식의 소프트 삭제로 처리된다.
- 이미지 업로드 성공 후 트랜잭션 실패 시 미사용 이미지 정리 대상이 기록된다.

### 8.1 구현 착수 전 필수 확정 항목

다음 항목은 개발 착수 전에 확정되어야 한다.

| 항목 | 확정값 |
|---|---|
| 세폴리아 Chain ID | `0xaa36a7` |
| 관리자 권한 | 컨트랙트 배포자 주소 고정 |
| 후보자 삭제 방식 | `isActive=false` 소프트 삭제 |
| 이미지 저장 방식 | 서버 저장 |
| 이미지 업로드 인증 | 관리자 지갑 서명 검증 기반 |
| 후보자 수 제한 | 1차 버전 최대 50명 |
| 득표수 공개 | 진행 중 및 종료 후 공개 |
| 투표 수 | 단일 투표 |

## 9. 기술적 구현 가능성 검증

### 9.1 종합 판단

본 문서의 요구사항은 현재 이더리움 세폴리아 네트워크, 메타마스크 브라우저 확장 프로그램, 웹 프론트엔드, 백엔드 서버, Solidity 스마트 컨트랙트 조합으로 구현 가능하다.

문서에는 다음 설계 기준이 반영되어 있으며, 구현 시에도 동일한 기준을 유지해야 한다.

- 후보자 사진은 서버 저장이므로 온체인 데이터와 서버 파일 간 정합성 관리가 필요하다.
- 이미지 업로드 API는 프론트엔드 표시만으로 관리자 권한을 제어해서는 안 되며, 백엔드에서도 관리자 인증이 필요하다.
- 후보자 삭제는 실제 배열 삭제가 아니라 `isActive=false` 방식의 소프트 삭제로 처리한다.
- 지갑 주소 기준 1인 1투표는 기술적으로 가능하지만, 실제 사람 기준 1인 1투표는 보장하지 않는다.
- 진행 중 득표수 공개는 기술적으로 가능하며, 공개 블록체인 특성과도 일치한다.

### 9.2 항목별 검증 결과

| 항목 | 구현 가능 여부 | 검증 결과 |
|---|---:|---|
| 세폴리아 네트워크 연동 | 가능 | 세폴리아는 애플리케이션 개발용 테스트넷으로 사용 가능하다. |
| 메타마스크 지갑 연결 | 가능 | `window.ethereum` 및 `eth_requestAccounts` 기반으로 구현 가능하다. |
| 세폴리아 네트워크 확인 | 가능 | `eth_chainId`로 현재 네트워크를 확인할 수 있으며, 세폴리아 Chain ID는 `0xaa36a7`이다. |
| 후보자 등록 | 가능 | 후보자 이름과 이미지 URL을 스마트 컨트랙트에 저장 가능하다. |
| 후보자 사진 서버 저장 | 가능 | 파일은 서버에 저장하고, 컨트랙트에는 URL 또는 파일 식별자만 저장하는 방식이 적합하다. |
| 후보자 수정 | 가능 | 투표 시작 전 상태에서만 `updateCandidate`를 허용하면 된다. |
| 후보자 삭제 | 가능 | `isActive=false`로 비활성화하고 조회 시 제외하는 방식이 적합하다. |
| 후보자 최소 2명 조건 | 가능 | 활성 후보자 수를 기준으로 `startVoting`에서 검증 가능하다. |
| 관리자 수동 시작/종료 | 가능 | `onlyOwner` 형태의 권한 검증으로 구현 가능하다. |
| 배포자 주소 관리자 고정 | 가능 | 컨트랙트 배포 시 `owner = msg.sender`로 설정하면 된다. |
| 중복 투표 방지 | 가능 | `mapping(address => bool)`로 투표 여부를 저장하면 된다. |
| 지갑 주소 기준 1인 1투표 | 가능 | 동일 주소 재투표는 차단 가능하나, 다중 지갑 사용자는 차단할 수 없다. |
| 진행 중 득표수 공개 | 가능 | 후보자별 `voteCount`를 조회해 화면에 표시하면 된다. |
| 종료 후 최종 결과 확인 | 가능 | 종료 상태에서도 동일한 득표수 데이터를 조회하면 된다. |
| 웹 브라우저 접근 | 가능 | 일반 웹 애플리케이션으로 구현 가능하다. |

### 9.3 구현 시 유지해야 할 필수 설계 기준

#### 9.3.1 이미지 업로드 권한 검증

이미지 업로드는 관리자만 수행할 수 있어야 하며, 백엔드 서버는 프론트엔드 화면 표시만 신뢰해서는 안 된다.

본 문서는 관리자 지갑 서명 기반 업로드 권한 검증을 기준으로 한다.

- 관리자 지갑 주소로 nonce 메시지 서명을 요청한다.
- 백엔드는 서명 복구 주소가 `ADMIN_ADDRESS`와 일치하는지 검증한다.
- `ADMIN_ADDRESS`는 컨트랙트 `owner()` 값과 일치해야 한다.
- 검증 성공 시에만 이미지 업로드 토큰을 발급한다.

#### 9.3.2 이미지와 트랜잭션 정합성

후보자 등록/수정은 이미지 업로드와 스마트 컨트랙트 트랜잭션이 분리되어 있다. 따라서 다음 예외가 발생할 수 있다.

| 상황 | 대응 방안 |
|---|---|
| 이미지 업로드 성공 후 트랜잭션 실패 | 미사용 이미지 파일로 기록하고 정리 대상에 포함 |
| 이미지 업로드 실패 | 컨트랙트 트랜잭션 실행 금지 |
| 이미지 URL 저장 후 서버 파일 삭제 | 이미지 표시 실패 방지를 위한 파일 보존 정책 필요 |
| 후보자 사진 수정 후 기존 이미지 미사용 | 기존 이미지를 즉시 삭제하지 않고 보관 후 정리 |

#### 9.3.3 후보자 삭제 구현 방식

스마트 컨트랙트에서 배열 원소를 실제로 삭제하면 후보자 ID가 변경되거나 조회 로직이 복잡해질 수 있다. 따라서 본 시스템은 다음 방식을 적용한다.

- 후보자 데이터에는 `isActive` 필드를 둔다.
- 삭제 시 `isActive=false`로 변경한다.
- `getCandidates()`는 활성 후보자만 반환하거나, 프론트엔드가 비활성 후보자를 제외한다.
- `startVoting()`은 활성 후보자 수가 2명 이상인지 확인한다.
- `vote(candidateId)`는 해당 후보자가 활성 상태인지 확인한다.

#### 9.3.4 후보자 수와 조회 성능

본 시스템은 단일 투표와 최대 후보자 수 50명을 전제로 하므로 `getCandidates()`로 전체 후보자를 반환하는 방식이 가능하다. 후보자 수 제한을 늘릴 경우에는 다음 보강이 필요하다.

- 후보자 수 제한
- 페이지네이션 조회 함수
- 이벤트 로그 기반 후보자 목록 구성

1차 버전에서는 후보자 수 50명 제한을 유지하므로 전체 목록 조회 방식을 기준으로 한다.

#### 9.3.5 관리자 주소 고정의 배포 방식

컨트랙트를 관리자가 직접 배포하면 `owner = msg.sender` 방식으로 관리자 주소 고정이 가능하다. 단, 배포 스크립트나 팩토리 컨트랙트를 통해 배포하는 경우 `msg.sender`가 실제 관리자 주소가 아닐 수 있으므로 배포 방식에 주의해야 한다.

#### 9.3.6 공개 투표의 보안/기획 영향

진행 중 득표수 공개는 기술적으로 가능하다. 또한 블록체인 데이터는 기본적으로 공개 조회가 가능하므로 공개 투표 정책과 잘 맞는다.

다만 진행 중 득표수 공개는 유권자 행동에 영향을 줄 수 있으므로, 본 시스템은 비밀 투표나 비공개 개표가 필요한 선거에는 적합하지 않다.

### 9.4 권장 기술 구성

| 영역 | 권장 구성 |
|---|---|
| 스마트 컨트랙트 | Solidity 0.8.x, OpenZeppelin Ownable 또는 직접 owner 구현 |
| 프론트엔드 | React 또는 Next.js, ethers.js 또는 viem |
| 지갑 연동 | MetaMask EIP-1193 provider |
| 네트워크 | Ethereum Sepolia, Chain ID `0xaa36a7` |
| 백엔드 | Node.js 기반 API 서버 또는 동일 수준의 파일 업로드 지원 서버 |
| 이미지 저장 | 서버 파일 시스템 또는 서버가 관리하는 정적 파일 저장소 |
| 테스트 | Hardhat 또는 Foundry 기반 컨트랙트 테스트 |

### 9.5 최종 검증 결론

본 문서의 기능 명세는 기술적으로 구현 가능하다. 1차 버전 구현에 치명적인 불가능 항목은 없다.

본 문서는 다음 필수 보강 항목을 포함하도록 갱신되었으므로, 현재 상태는 실제 개발 착수가 가능한 수준으로 판단한다.

1. 백엔드 이미지 업로드 API의 관리자 권한 검증
2. 이미지 업로드와 컨트랙트 트랜잭션 간 정합성 처리
3. 후보자 삭제를 `isActive` 기반 소프트 삭제로 구현
4. 스마트 컨트랙트 이벤트 정의
5. 필수 환경 변수와 네트워크 식별값 정의
6. 후보자 수 제한값 정의

위 항목이 문서에 반영되어 있으므로 현재 PRD/SRS는 구현 가능한 수준의 기능 명세로 볼 수 있다.

### 9.6 검증 참고 자료

- ethereum.org Networks 문서: 세폴리아는 애플리케이션 개발용 테스트넷으로 안내되어 있으며, 테스트넷 ETH와 faucet 사용이 필요하다.
- MetaMask 문서: dApp은 `eth_requestAccounts`로 사용자 계정 접근을 요청할 수 있으며, 요청은 사용자 버튼 클릭 같은 직접 동작에 의해 시작해야 한다.
- MetaMask 문서: 현재 네트워크는 `eth_chainId`로 확인할 수 있으며, 세폴리아 Chain ID는 `0xaa36a7`이다.

## 10. 문서 일관성 점검 결과

### 10.1 점검 결론

본 문서는 PRD, SRS, 화면 설계서, 사용자 시나리오, 테스트 기준, 구현 가능성 검증이 동일한 정책을 기준으로 서술되도록 정리되었다.

### 10.2 일관성 기준

| 점검 항목 | 최종 기준 | 적용 상태 |
|---|---|---|
| 투표 상태명 | 화면은 `준비 전/진행 중/종료`, 컨트랙트는 `Ready/Active/Ended` | 반영 |
| 투표 시작 조건 | 활성 후보자 2명 이상 | 반영 |
| 후보자 삭제 | `isActive=false` 소프트 삭제 | 반영 |
| 후보자 ID | 발급 후 재사용하지 않음 | 반영 |
| 관리자 권한 | 컨트랙트 배포자 주소 고정 | 반영 |
| 이미지 업로드 인증 | 관리자 지갑 서명 검증 기반 | 반영 |
| 서버 관리자 주소 | `ADMIN_ADDRESS`와 컨트랙트 `owner()` 일치 필요 | 반영 |
| 이미지 저장 | 서버 저장, 컨트랙트에는 URL 또는 파일 식별자 저장 | 반영 |
| 이미지 정합성 | 업로드와 트랜잭션 실패 케이스 분리 처리 | 반영 |
| 득표수 공개 | 진행 중 및 종료 후 모두 공개 | 반영 |
| 투표 운영 방식 | 단일 투표 | 반영 |
| 후보자 수 제한 | 1차 버전 최대 50명 | 반영 |

### 10.3 보완된 불일치 항목

| 기존 불일치 | 보완 내용 |
|---|---|
| `후보자 2명 이상`과 `활성 후보자 2명 이상` 표현 혼재 | 문서 표기 기준을 추가하고 투표 시작 조건을 활성 후보자 기준으로 통일 |
| 화면 상태명과 컨트랙트 상태값 혼재 | 한글 상태와 enum 상태값 매핑표 추가 |
| 후보자 삭제가 실제 삭제처럼 읽힐 수 있음 | 소프트 삭제 정책과 `isActive=false` 기준 명시 |
| 이미지 수정 시 항상 새 사진 업로드가 필요한 것처럼 서술 | 수정 시 기존 사진 유지 또는 새 사진 업로드로 정리 |
| 이미지 업로드 권한 검증 방식이 선택지처럼 서술 | 관리자 지갑 서명 검증 방식으로 통일 |
| 서버의 관리자 기준이 모호함 | `ADMIN_ADDRESS`와 컨트랙트 `owner()` 일치 조건 명시 |
| 후보자 수 제한이 권장값처럼 서술 | 1차 버전 최대 50명 제한으로 통일 |

### 10.4 잔여 주의사항

다음 사항은 문서 내 정책 충돌은 아니지만 구현 시 주의가 필요하다.

- 세폴리아 테스트넷 ETH가 없는 사용자는 트랜잭션을 실행할 수 없다.
- 지갑 주소 기준 1인 1투표는 실제 사람 기준 중복 투표를 막지 못한다.
- 공개 투표 정책이므로 진행 중 득표수 공개가 유권자 행동에 영향을 줄 수 있다.
- 서버 이미지 파일을 임의로 삭제하면 컨트랙트에 저장된 이미지 URL이 깨질 수 있다.

## 11. 구현 계획

### 11.1 구현 계획 개요

본 구현 계획은 1인 개발자가 순차적으로 개발하는 것을 전제로 한다. 각 Phase는 약 1 man/day 규모로 구성하며, 앞 Phase의 산출물이 뒤 Phase의 기반이 되도록 순서를 배치한다.

각 Phase는 다음 공통 활동을 반드시 포함한다.

- 구현
- 코드리뷰
- 검증
- 테스트
- 사이드 이펙트 검증
- 지금까지 구현된 모든 Phase의 누적 테스트

1인 개발 환경에서는 코드리뷰를 셀프 리뷰로 수행하되, 체크리스트 기반으로 진행한다. 리뷰 시 기능 구현 여부뿐 아니라 요구사항 누락, 권한 검증, 예외 처리, 테스트 누락 여부를 함께 확인한다.

### 11.2 Phase 1: 프로젝트 기반 구성 및 개발 환경 준비

#### 목표

프론트엔드, 백엔드, 스마트 컨트랙트 개발을 시작할 수 있는 기본 프로젝트 구조와 실행 환경을 구성한다.

#### 구현

- 프로젝트 저장소 구조 정의
- 프론트엔드 프로젝트 생성
- 백엔드 서버 프로젝트 생성
- 스마트 컨트랙트 개발 환경 구성
- 환경 변수 구조 정의
- 로컬 실행 스크립트 구성
- 기본 README 또는 실행 가이드 작성

#### 코드리뷰

- 디렉터리 구조가 프론트엔드, 백엔드, 컨트랙트 책임을 명확히 분리하는지 확인
- 환경 변수 예시 파일에 민감 정보가 포함되지 않았는지 확인
- 실행 스크립트가 1인 개발자가 반복 사용하기 쉬운지 확인

#### 검증

- 프론트엔드 로컬 서버가 실행되는지 확인
- 백엔드 서버가 실행되는지 확인
- 컨트랙트 테스트 명령이 실행 가능한지 확인

#### 테스트

- 기본 빌드 또는 타입 체크 실행
- 기본 백엔드 헬스 체크 API 테스트
- 빈 컨트랙트 또는 샘플 컨트랙트 컴파일 테스트

#### 사이드 이펙트 검증

- 불필요한 글로벌 설정 변경이 없는지 확인
- 환경 변수 누락 시 명확한 오류가 발생하는지 확인

#### 누적 테스트

- Phase 1 전체 실행 절차 재현

#### 완료 기준

- 프론트엔드, 백엔드, 컨트랙트 개발 환경이 모두 실행 가능하다.
- 환경 변수와 실행 방법이 문서화되어 있다.

### 11.3 Phase 2: 스마트 컨트랙트 상태 및 후보자 관리 구현

#### 목표

투표 상태 모델과 후보자 등록, 수정, 삭제 기능을 스마트 컨트랙트에 구현한다.

#### 구현

- `Ready`, `Active`, `Ended` 상태 enum 구현
- `owner` 관리자 주소 설정
- 후보자 구조체 구현
- `candidateId`, `name`, `imageUrl`, `voteCount`, `isActive` 필드 구현
- `addCandidate` 구현
- `updateCandidate` 구현
- `deleteCandidate` 구현
- 후보자 수 제한 50명 구현
- 후보자 관련 이벤트 구현

#### 코드리뷰

- 관리자 권한 검증이 모든 후보자 관리 함수에 적용됐는지 확인
- 후보자 삭제가 실제 삭제가 아니라 `isActive=false`로 처리되는지 확인
- 후보자 ID가 재사용되지 않는지 확인
- 이름과 이미지 URL 빈 값 검증이 있는지 확인

#### 검증

- 관리자만 후보자 등록, 수정, 삭제를 할 수 있는지 확인
- 비관리자 호출이 revert 되는지 확인
- 삭제된 후보자가 목록에서 제외되는지 확인
- 후보자 수 제한 초과 시 등록이 실패하는지 확인

#### 테스트

- 후보자 등록 성공 테스트
- 후보자 수정 성공 테스트
- 후보자 삭제 성공 테스트
- 비관리자 후보자 관리 실패 테스트
- 후보자 수 50명 초과 실패 테스트
- 삭제 후보자 재삭제 실패 테스트

#### 사이드 이펙트 검증

- 삭제된 후보자의 ID가 다른 후보자 ID에 영향을 주지 않는지 확인
- 후보자 수정이 득표수에 영향을 주지 않는지 확인

#### 누적 테스트

- Phase 1 실행 환경 테스트
- Phase 2 컨트랙트 후보자 관리 테스트 전체 실행

#### 완료 기준

- 후보자 등록, 수정, 삭제가 컨트랙트 테스트로 검증된다.
- 후보자 상태와 이벤트가 요구사항과 일치한다.

### 11.4 Phase 3: 스마트 컨트랙트 투표 시작, 투표, 종료 구현

#### 목표

투표 시작, 투표 참여, 중복 투표 방지, 투표 종료 기능을 스마트 컨트랙트에 구현한다.

#### 구현

- `startVoting` 구현
- `vote` 구현
- `endVoting` 구현
- `hasVoted` 구현
- 투표자 선택 후보자 기록 구현
- 활성 후보자 2명 이상 시작 조건 구현
- 중복 투표 방지 mapping 구현
- 투표 및 상태 변경 이벤트 구현

#### 코드리뷰

- 투표 시작 조건이 전체 후보자 수가 아니라 활성 후보자 수 기준인지 확인
- `vote`가 `Active` 상태에서만 가능한지 확인
- 삭제된 후보자에게 투표할 수 없는지 확인
- 동일 지갑 주소 중복 투표가 컨트랙트 레벨에서 차단되는지 확인
- 종료 후 투표가 불가능한지 확인

#### 검증

- 후보자 2명 미만 상태에서 투표 시작 실패 확인
- 활성 후보자 2명 이상 상태에서 투표 시작 성공 확인
- 투표 성공 시 득표수 증가 확인
- 중복 투표 실패 확인
- 투표 종료 후 추가 투표 실패 확인

#### 테스트

- 투표 시작 성공/실패 테스트
- 투표 성공 테스트
- 중복 투표 실패 테스트
- 삭제 후보자 투표 실패 테스트
- 투표 종료 성공 테스트
- 종료 후 투표 실패 테스트
- 비관리자 종료 실패 테스트

#### 사이드 이펙트 검증

- 후보자 관리 기능이 `Active`, `Ended` 상태에서 차단되는지 확인
- 투표 종료가 기존 득표수를 변경하지 않는지 확인
- `hasVoted` 값이 투표 전후 정확히 변경되는지 확인

#### 누적 테스트

- Phase 1 실행 환경 테스트
- Phase 2 후보자 관리 테스트 전체 실행
- Phase 3 투표 상태 및 투표 테스트 전체 실행

#### 완료 기준

- 스마트 컨트랙트의 핵심 투표 플로우가 테스트로 검증된다.
- 단일 투표, 1지갑 1투표, 공개 득표수 정책이 컨트랙트 레벨에서 보장된다.

### 11.5 Phase 4: 백엔드 이미지 업로드 및 관리자 서명 검증 구현

#### 목표

후보자 사진을 서버에 저장하고, 관리자 지갑 서명 검증 기반으로 업로드 권한을 제어한다.

#### 구현

- `/api/auth/nonce` API 구현
- `/api/auth/verify` API 구현
- 관리자 서명 검증 구현
- 업로드 토큰 발급 구현
- `/api/candidate-images` 이미지 업로드 API 구현
- `/uploads/{fileName}` 정적 이미지 조회 구현
- 파일 형식, MIME 타입, 크기 검증 구현
- 미사용 이미지 기록 구조 구현

#### 코드리뷰

- 이미지 업로드 API가 관리자 업로드 토큰 없이 호출되지 않는지 확인
- `ADMIN_ADDRESS`와 서명 복구 주소 비교가 정확한지 확인
- 파일 확장자와 MIME 타입을 모두 검증하는지 확인
- 파일명이 서버에서 고유하게 생성되는지 확인
- 업로드 실패 시 불완전 파일이 남지 않는지 확인

#### 검증

- 비관리자 서명으로 업로드 토큰 발급이 실패하는지 확인
- 관리자 서명으로 업로드 토큰 발급이 성공하는지 확인
- 허용되지 않은 파일 형식 업로드가 실패하는지 확인
- 5MB 초과 파일 업로드가 실패하는지 확인
- 업로드 성공 시 이미지 URL이 접근 가능한지 확인

#### 테스트

- nonce 발급 테스트
- 서명 검증 성공/실패 테스트
- 이미지 업로드 성공 테스트
- 잘못된 확장자 업로드 실패 테스트
- 잘못된 MIME 타입 업로드 실패 테스트
- 파일 크기 초과 실패 테스트
- 이미지 조회 테스트

#### 사이드 이펙트 검증

- 업로드 토큰 만료 처리가 기존 정상 업로드를 방해하지 않는지 확인
- 이미지 업로드 실패가 서버 프로세스를 중단시키지 않는지 확인
- 정적 파일 공개 경로가 서버 내부 경로를 노출하지 않는지 확인

#### 누적 테스트

- Phase 1 실행 환경 테스트
- Phase 2, 3 컨트랙트 테스트 전체 실행
- Phase 4 백엔드 API 테스트 전체 실행

#### 완료 기준

- 관리자만 후보자 이미지를 업로드할 수 있다.
- 업로드된 이미지는 URL로 접근 가능하다.
- 잘못된 파일 업로드가 서버에서 차단된다.

### 11.6 Phase 5: 프론트엔드 지갑 연결 및 네트워크 검증 구현

#### 목표

웹 브라우저에서 메타마스크 지갑 연결, 세폴리아 네트워크 검증, 관리자 식별을 구현한다.

#### 구현

- 메타마스크 설치 여부 확인
- 지갑 연결 버튼 구현
- `eth_requestAccounts` 계정 연결 구현
- `eth_chainId` 네트워크 확인 구현
- 세폴리아 Chain ID `0xaa36a7` 검증 구현
- 네트워크 전환 안내 구현
- 컨트랙트 `owner()` 조회 또는 `ADMIN_ADDRESS` 기준 관리자 표시 구현
- 지갑 주소 축약 표시 구현

#### 코드리뷰

- 사용자 액션 없이 지갑 연결 요청이 자동 실행되지 않는지 확인
- 세폴리아가 아닌 경우 트랜잭션 버튼이 비활성화되는지 확인
- 관리자 표시가 프론트엔드 편의 기능이며 실제 권한은 컨트랙트/서버에서 검증되는지 확인

#### 검증

- 메타마스크 미설치 상태 메시지 확인
- 지갑 연결 거부 시 오류 메시지 확인
- 세폴리아 외 네트워크에서 안내 메시지 확인
- 관리자 주소 접속 시 관리자 표시 확인
- 일반 사용자 주소 접속 시 관리자 기능 숨김 또는 비활성화 확인

#### 테스트

- 지갑 미연결 상태 화면 테스트
- 지갑 연결 성공 상태 테스트
- 네트워크 불일치 상태 테스트
- 관리자/일반 사용자 화면 분기 테스트

#### 사이드 이펙트 검증

- 계정 변경 시 화면 상태가 갱신되는지 확인
- 네트워크 변경 시 화면 상태가 갱신되는지 확인
- 지갑 미연결 사용자가 조회 화면을 사용할 수 있는지 확인

#### 누적 테스트

- Phase 1 실행 환경 테스트
- Phase 2, 3 컨트랙트 테스트 전체 실행
- Phase 4 백엔드 API 테스트 전체 실행
- Phase 5 프론트엔드 지갑 연결 테스트 실행

#### 완료 기준

- 사용자가 메타마스크로 지갑을 연결할 수 있다.
- 세폴리아 네트워크 상태와 관리자 여부가 화면에 반영된다.

### 11.7 Phase 6: 프론트엔드 후보자 관리 화면 구현

#### 목표

관리자가 웹 화면에서 후보자를 등록, 수정, 삭제할 수 있도록 구현한다.

#### 구현

- 관리자 패널 구현
- 후보자 등록 폼 구현
- 후보자 이미지 선택 및 미리보기 구현
- 관리자 지갑 서명 기반 이미지 업로드 연동
- 후보자 등록 트랜잭션 연동
- 후보자 수정 UI 구현
- 후보자 삭제 확인 UI 구현
- 후보자 수정/삭제 트랜잭션 연동
- 후보자 목록 재조회 구현

#### 코드리뷰

- 준비 전 상태에서만 후보자 등록, 수정, 삭제 UI가 활성화되는지 확인
- 이미지 업로드 성공 후에만 후보자 등록/수정 트랜잭션이 실행되는지 확인
- 트랜잭션 실패 시 사용자에게 명확한 메시지가 표시되는지 확인
- 삭제가 소프트 삭제 정책과 일치하도록 처리되는지 확인

#### 검증

- 후보자 등록 후 목록에 표시되는지 확인
- 후보자 수정 후 이름과 사진이 갱신되는지 확인
- 후보자 삭제 후 목록에서 제외되는지 확인
- 일반 사용자가 후보자 관리 기능을 사용할 수 없는지 확인
- 투표 진행 중 후보자 관리 버튼이 비활성화되는지 확인

#### 테스트

- 후보자 등록 화면 테스트
- 이미지 미리보기 테스트
- 이미지 업로드 실패 처리 테스트
- 후보자 등록 트랜잭션 성공/실패 처리 테스트
- 후보자 수정 성공 테스트
- 후보자 삭제 성공 테스트
- 관리자 외 접근 제한 테스트

#### 사이드 이펙트 검증

- 후보자 수정이 기존 득표수에 영향을 주지 않는지 확인
- 삭제 후 활성 후보자 수가 투표 시작 버튼 상태에 반영되는지 확인
- 이미지 업로드 실패 파일이 후보자 목록에 반영되지 않는지 확인

#### 누적 테스트

- Phase 1 실행 환경 테스트
- Phase 2, 3 컨트랙트 테스트 전체 실행
- Phase 4 백엔드 API 테스트 전체 실행
- Phase 5 지갑 연결 테스트 실행
- Phase 6 후보자 관리 화면 테스트 실행

#### 완료 기준

- 관리자가 웹 화면에서 후보자를 등록, 수정, 삭제할 수 있다.
- 후보자 이미지가 서버에 저장되고 화면에 표시된다.

### 11.8 Phase 7: 프론트엔드 투표 시작, 투표 참여, 종료 구현

#### 목표

관리자의 투표 시작/종료와 유권자의 투표 참여 플로우를 웹 화면에서 구현한다.

#### 구현

- 투표 상태 조회 구현
- 활성 후보자 수 기반 투표 시작 버튼 상태 구현
- 투표 시작 트랜잭션 연동
- 후보자별 투표 버튼 구현
- 투표 참여 트랜잭션 연동
- `hasVoted` 조회 및 투표 완료 상태 표시
- 투표 종료 버튼 구현
- 투표 종료 트랜잭션 연동
- 트랜잭션 처리 중 로딩 상태 구현

#### 코드리뷰

- 투표 시작 조건이 활성 후보자 2명 이상 기준인지 확인
- 투표 버튼 활성화 조건이 문서와 일치하는지 확인
- 중복 투표 방지가 프론트엔드뿐 아니라 컨트랙트 오류 처리까지 포함하는지 확인
- 종료 후 투표 버튼이 비활성화되는지 확인

#### 검증

- 활성 후보자 2명 미만일 때 투표 시작 버튼 비활성화 확인
- 활성 후보자 2명 이상일 때 투표 시작 가능 확인
- 유권자 투표 성공 후 득표수 증가 확인
- 이미 투표한 사용자에게 투표 완료 상태 표시 확인
- 투표 종료 후 추가 투표 불가 확인

#### 테스트

- 투표 시작 성공/실패 화면 테스트
- 투표 참여 성공 테스트
- 중복 투표 시도 처리 테스트
- 투표 종료 성공 테스트
- 종료 후 투표 버튼 비활성화 테스트
- 트랜잭션 거부 메시지 테스트

#### 사이드 이펙트 검증

- 투표 후 후보자 목록과 득표수가 정상 재조회되는지 확인
- 계정 변경 시 `hasVoted` 상태가 새 계정 기준으로 갱신되는지 확인
- 투표 종료가 후보자 사진 표시나 후보자 목록 조회에 영향을 주지 않는지 확인

#### 누적 테스트

- Phase 1 실행 환경 테스트
- Phase 2, 3 컨트랙트 테스트 전체 실행
- Phase 4 백엔드 API 테스트 전체 실행
- Phase 5 지갑 연결 테스트 실행
- Phase 6 후보자 관리 화면 테스트 실행
- Phase 7 투표 플로우 테스트 실행

#### 완료 기준

- 관리자가 투표를 시작하고 종료할 수 있다.
- 유권자가 후보자 1명에게 투표할 수 있다.
- 중복 투표와 종료 후 투표가 차단된다.

### 11.9 Phase 8: 득표수 공개, 결과 화면, 화면 갱신 구현

#### 목표

투표 진행 중 현재 득표수와 투표 종료 후 최종 결과를 사용자에게 명확히 표시한다.

#### 구현

- 후보자별 현재 득표수 표시 구현
- 트랜잭션 완료 후 후보자 목록 재조회 구현
- 일정 주기 polling 구현
- 종료 후 최종 결과 화면 구현
- 최다 득표자 계산 구현
- 동점 표시 구현
- 공개 투표 안내 문구 정리

#### 코드리뷰

- 진행 중 득표수와 종료 후 최종 득표수가 동일한 컨트랙트 데이터를 기준으로 표시되는지 확인
- 최다 득표자 계산이 동점 케이스를 처리하는지 확인
- polling이 과도한 RPC 요청을 만들지 않는지 확인

#### 검증

- 투표 진행 중 득표수가 표시되는지 확인
- 투표 후 득표수가 갱신되는지 확인
- 투표 종료 후 최종 결과가 표시되는지 확인
- 동점 상황에서 동점 표시가 되는지 확인

#### 테스트

- 진행 중 득표수 표시 테스트
- 투표 후 재조회 테스트
- polling 갱신 테스트
- 최종 결과 표시 테스트
- 최다 득표자 표시 테스트
- 동점 표시 테스트

#### 사이드 이펙트 검증

- polling이 트랜잭션 처리 중 화면 상태를 덮어쓰지 않는지 확인
- 종료 후 결과 화면에서도 후보자 이미지가 정상 표시되는지 확인
- 지갑 미연결 사용자도 득표수와 결과를 조회할 수 있는지 확인

#### 누적 테스트

- Phase 1 실행 환경 테스트
- Phase 2, 3 컨트랙트 테스트 전체 실행
- Phase 4 백엔드 API 테스트 전체 실행
- Phase 5 지갑 연결 테스트 실행
- Phase 6 후보자 관리 화면 테스트 실행
- Phase 7 투표 플로우 테스트 실행
- Phase 8 결과 화면 테스트 실행

#### 완료 기준

- 진행 중 득표수와 종료 후 최종 결과가 화면에 정확히 표시된다.
- 동점과 최다 득표자 표시가 정상 동작한다.

### 11.10 Phase 9: 통합 검증 및 세폴리아 배포 준비

#### 목표

로컬 개발 환경에서 전체 기능을 통합 검증하고, 세폴리아 배포에 필요한 설정과 절차를 정리한다.

#### 구현

- 컨트랙트 배포 스크립트 작성
- 세폴리아 배포 환경 변수 정리
- 프론트엔드 컨트랙트 주소 설정 연동
- 백엔드 `ADMIN_ADDRESS` 설정 검증 로직 보완
- 배포 후 컨트랙트 `owner()` 확인 절차 작성
- 통합 실행 가이드 작성

#### 코드리뷰

- 배포 스크립트가 의도한 관리자 지갑으로 실행되는지 확인
- 배포된 컨트랙트 주소가 프론트엔드 환경 변수에 정확히 반영되는지 확인
- 서버 `ADMIN_ADDRESS`와 컨트랙트 `owner()` 불일치 시 업로드가 차단되는지 확인

#### 검증

- 로컬 통합 시나리오 전체 수행
- 테스트넷 배포 전 환경 변수 누락 확인
- 세폴리아 RPC 연결 확인
- 테스트넷 ETH 필요 여부 확인

#### 테스트

- 전체 컨트랙트 테스트 실행
- 전체 백엔드 API 테스트 실행
- 주요 프론트엔드 화면 테스트 실행
- 로컬 E2E 시나리오 수동 테스트

#### 사이드 이펙트 검증

- 배포 환경 설정이 로컬 개발 환경을 깨뜨리지 않는지 확인
- 컨트랙트 주소 변경 후 프론트엔드 캐시나 이전 상태가 남지 않는지 확인
- 서버 관리자 주소 변경 시 기존 업로드 토큰이 무효화되는지 확인

#### 누적 테스트

- Phase 1부터 Phase 9까지 전체 테스트 실행
- 후보자 등록부터 투표 종료 및 결과 확인까지 전체 사용자 흐름 테스트

#### 완료 기준

- 세폴리아 배포 전 필요한 설정과 검증 절차가 준비되어 있다.
- 로컬 통합 환경에서 전체 투표 플로우가 정상 동작한다.

### 11.11 Phase 10: 세폴리아 배포 및 최종 인수 테스트

#### 목표

세폴리아 네트워크에 스마트 컨트랙트를 배포하고, 실제 메타마스크 연동 환경에서 최종 인수 테스트를 수행한다.

#### 구현

- 세폴리아에 스마트 컨트랙트 배포
- 배포된 컨트랙트 주소 기록
- 컨트랙트 `owner()` 확인
- 프론트엔드 `CONTRACT_ADDRESS` 반영
- 백엔드 `ADMIN_ADDRESS` 반영
- 서버 및 프론트엔드 배포
- 최종 운영 환경 smoke test 수행

#### 코드리뷰

- 배포 주소와 환경 변수 값이 일치하는지 확인
- 컨트랙트 owner가 실제 관리자 지갑 주소인지 확인
- 운영 환경에 민감 정보가 노출되지 않았는지 확인
- 테스트넷용 설정이 문서와 일치하는지 확인

#### 검증

- 메타마스크 세폴리아 연결 확인
- 관리자 이미지 업로드 확인
- 후보자 등록, 수정, 삭제 확인
- 투표 시작 확인
- 유권자 투표 확인
- 중복 투표 차단 확인
- 투표 종료 및 최종 결과 확인

#### 테스트

- 세폴리아 환경 후보자 관리 테스트
- 세폴리아 환경 투표 플로우 테스트
- 일반 사용자 조회 테스트
- 관리자 외 사용자 권한 차단 테스트
- 이미지 표시 테스트
- 트랜잭션 거부 및 실패 메시지 테스트

#### 사이드 이펙트 검증

- 여러 계정 전환 시 투표 여부 표시가 올바른지 확인
- 네트워크 전환 시 화면 상태가 올바르게 변경되는지 확인
- 이미지 업로드 실패가 컨트랙트 상태를 변경하지 않는지 확인
- 세폴리아 트랜잭션 지연 중 중복 클릭이 방지되는지 확인

#### 누적 테스트

- Phase 1부터 Phase 10까지 가능한 자동 테스트 전체 실행
- 세폴리아 환경에서 전체 인수 시나리오 수동 테스트
- 관리자 계정과 일반 유권자 계정을 분리한 최종 검증

#### 완료 기준

- 세폴리아 네트워크에서 전체 투표 시스템이 정상 동작한다.
- 문서의 승인 기준을 모두 만족한다.
- 개발자가 재배포 및 재검증 절차를 문서대로 수행할 수 있다.

### 11.12 Phase별 산출물 요약

| Phase | 주요 산출물 | 예상 규모 |
|---|---|---:|
| Phase 1 | 프로젝트 구조, 실행 환경, 환경 변수 기본 구조 | 1 man/day |
| Phase 2 | 후보자 관리 스마트 컨트랙트 | 1 man/day |
| Phase 3 | 투표 플로우 스마트 컨트랙트 | 1 man/day |
| Phase 4 | 이미지 업로드 백엔드와 관리자 서명 검증 | 1 man/day |
| Phase 5 | 지갑 연결, 네트워크 검증, 관리자 식별 UI | 1 man/day |
| Phase 6 | 후보자 등록, 수정, 삭제 화면 | 1 man/day |
| Phase 7 | 투표 시작, 투표 참여, 투표 종료 화면 | 1 man/day |
| Phase 8 | 득표수 공개, 결과 화면, 화면 갱신 | 1 man/day |
| Phase 9 | 통합 검증 및 세폴리아 배포 준비 | 1 man/day |
| Phase 10 | 세폴리아 배포 및 최종 인수 테스트 | 1 man/day |

총 예상 규모는 약 10 man/day이다. 단, 세폴리아 네트워크 상태, 메타마스크 트랜잭션 지연, 배포 환경 준비 상태, 프론트엔드 디자인 완성도 요구 수준에 따라 일정은 늘어날 수 있다.
